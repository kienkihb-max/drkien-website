_template/
*.zip
# Bản sao repo do Claude Code tạo khi chạy nhiều phiên song song —
# không phải nội dung site, lỡ commit vào là nhân đôi cả kho.
.claude/worktrees/
.DS_Store
Thumbs.db

# ——— Phần Astro + Supabase ———
web/node_modules/
web/dist/
web/.astro/
# Bản sao tài nguyên tĩnh do web/dong-bo-tinh.mjs tự chép từ gốc repo sang.
# Nguồn duy nhất vẫn là ảnh và style ở gốc — commit thêm bản sao là kho
# phình gấp đôi và từ đó có hai bản của mỗi tấm ảnh.
web/public/
# Nhật ký lỗi trình duyệt do máy chủ dev ghi ra, mỗi lần chạy tạo lại.
web/nhat-ky-dev.log
# Khoá Supabase của từng máy. Bản mẫu là web/.env.example, có commit.
web/.env
