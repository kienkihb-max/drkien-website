# Những thứ KHÔNG gửi lên khi `gcloud run deploy --source agent-proxy`.
# Thư viện Cloud Run tự cài lại từ package-lock.json; ghi chú và prompt của
# agent không cần cho máy chủ chạy.
.gcloudignore
node_modules/
*.md
